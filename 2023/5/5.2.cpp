#include <iostream>
#include <cstdio>
#include <vector>
#include <limits>
#include <algorithm>
#include <set>
#include <unordered_set>
using namespace std;

int main(){
    freopen("5", "r", stdin);
    int i = -1;
    pair<pair<int, int>, int> arr[100000];
    for (int j = 0; j < 224; j++) {
        string s;
        cin >> s;
        if (s.empty()) {
            continue;
        }
        if(s[s.length()-1] == ':') {
            
        }
    }
}